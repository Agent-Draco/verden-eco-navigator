import { describe, it, expect } from 'vitest';

describe.skip('formatInstruction (Module deleted)', () => {
  it('tests removed because module was deleted', () => {
    expect(true).toBe(true);
  });
});
