## 2024-05-18 - Semantic Navigation States

**Learning:** Navigation components need semantic `aria-current="page"` states for active routes, and all interactive elements should have standard keyboard focus indicators (`focus-visible:ring`).
**Action:** Add `aria-current` to active nav links and standard `focus-visible` utility classes to all buttons for consistent screen-reader and keyboard accessibility.
