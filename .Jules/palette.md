## 2026-06-25 - Semantic Navigation & Focus Indicators

**Learning:** Navigation components often rely only on visual cues for active states. Semantic accessibility requires combining visual indicators with `aria-current="page"` and explicit keyboard focus rings for screen readers and keyboard users.
**Action:** Standardize all navigation components to pair visual active states with `aria-current` and `focus-visible` utility classes.
