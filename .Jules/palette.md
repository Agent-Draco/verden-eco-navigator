## 2025-03-01 - Password Toggle Accessibility Fix
**Learning:** Icon-only toggle buttons require dynamic `aria-label`s, `type="button"`, `aria-hidden` on the icons, and explicit `focus-visible` outline styles for keyboard navigation.
**Action:** Apply this pattern to all icon-only interactive elements, especially password visibility toggles.
