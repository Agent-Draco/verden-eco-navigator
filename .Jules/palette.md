## 2024-07-01 - Accessible Navigation Standardization

**Learning:** Navigation components like sidebars and bottom bars often lack semantic states (e.g., `aria-current`) and keyboard focus indicators, excluding screen-reader and keyboard users.
**Action:** Standardized navigation components to always include `aria-current="page"` on active routes and visual `focus-visible` outlines for accessibility consistency.
