## 2024-03-20 - Navigation Accessibility Pattern
**Learning:** For interactive navigation elements, applying dynamic `aria-current="page"` and explicit `focus-visible` styles ensures a seamless keyboard accessibility experience while preserving the intended mouse/touch UX.
**Action:** Always include keyboard focus rings (`focus-visible`) and stateful ARIA attributes (`aria-current`) on reusable navigation components.
