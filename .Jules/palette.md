## 2026-06-03 - Enhanced Navigation Accessibility

**Learning:** Many interactive components lack explicit `aria-current` attributes and clear `focus-visible` outlines, which hinders keyboard and screen reader accessibility.
**Action:** Applied `aria-current` to active routes and added comprehensive `focus-visible` utility classes to ensure elements are clearly distinguishable for keyboard users without affecting pointer interactions.
