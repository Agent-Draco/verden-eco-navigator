
## 2024-07-04 - Semantic Navigation Accessibility
**Learning:** Standardizing navigation components requires both visual focus indicators and semantic screen reader cues (`aria-current="page"`) to ensure the active route is communicated effectively.
**Action:** Always pair visual active states with `aria-current` attributes and include `focus-visible` styles on custom interactive navigation elements.
