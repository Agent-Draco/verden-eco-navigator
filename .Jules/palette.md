## 2024-05-30 - Added ARIA current and keyboard focus to navigation

**Learning:** Using `aria-current="page"` dynamically conditionally improves accessibility for screen readers and `focus-visible` styling ensures a usable experience without causing disruptive rings for mouse users.
**Action:** Apply `aria-current` dynamically and explicit `focus-visible` rings for navigation components across all repos where missing.
