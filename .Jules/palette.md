## 2026-07-01 - Standardized navigation accessibility
**Learning:** Interactive navigation components require semantic ARIA states like `aria-current="page"` alongside clear visual focus indicators (`focus-visible`) and keyboard-accessible tooltips (`group-focus-visible`) for true accessibility.
**Action:** Ensure all future navigation components include `focus-visible` states and semantic ARIA attributes.
