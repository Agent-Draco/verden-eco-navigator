## 2026-05-27 - Add ARIA and keyboard focus to Navigation\n\n**Learning:** Navigation components often miss ARIA attributes (like `aria-current`) and distinct keyboard focus states (`focus-visible`).\n**Action:** Always dynamically add `aria-current={active ? 'page' : undefined}` and apply explicit `focus-visible` utility classes (e.g., `focus-visible:ring-2`) for accessibility without disrupting mouse users.
