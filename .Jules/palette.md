## 2026-06-18 - Accessible Icon Toggle Buttons

**Learning:** Icon-only toggle buttons (like password visibility) often lack screen-reader context and keyboard focus states.
**Action:** Always provide descriptive `aria-label` attributes, dynamic `aria-pressed` states, and consistent `focus-visible` styling for icon-only toggles.
