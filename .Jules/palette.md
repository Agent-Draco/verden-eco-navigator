## 2026-05-25 - Add accessibility enhancements to navigation buttons

**Learning:** Navigation buttons (especially in a sidebar/bottom nav) need explicit keyboard focus indicators (like `focus-visible:ring`) and `aria-current` applied conditionally to improve non-mouse usability without compromising the design.
**Action:** Ensure `focus-visible` is used systematically on interactive navigation components.
