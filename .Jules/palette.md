## 2024-07-15 - Add ARIA labels to icon-only transport buttons

**Learning:** Icon-only buttons used for primary navigation or mode selection often lack accessible names, making them difficult to use with screen readers.
**Action:** Always verify that icon-only `GlassButton` components or similar have an explicit `aria-label` attribute describing their function.
