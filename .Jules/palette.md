## 2024-05-30 - Enhance navigation accessibility with focus states and aria-current

**Learning:** Supplementing visual active states with `aria-current="page"` ensures screen readers accurately convey the current view, while combining it with explicit `focus-visible:ring` utilities guarantees robust keyboard navigation support without affecting pointer users.
**Action:** Establish this as a baseline accessibility pattern for custom interactive navigation components across projects.
