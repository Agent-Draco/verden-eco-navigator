## 2026-06-26 - BottomNav Accessibility Enhancement

**Learning:** Standardizing navigation components like BottomNav requires semantic ARIA attributes (`aria-current="page"`) alongside clear visual focus indicators (`focus-visible:ring-2`) to maintain screen-reader accessibility.
**Action:** Always ensure active routes utilize `aria-current` and implement standard keyboard focus indicators using Tailwind utility patterns.
