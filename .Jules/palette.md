## 2024-05-15 - [GlassButton Accessibility Enhancement]
**Learning:** Reusable button components often miss critical accessibility states like `focus-visible` and `disabled` opacities when utilizing custom animations and styles.
**Action:** Ensure custom interactive elements always include robust focus states and clear visual disabled states using Tailwind CSS classes like `focus-visible:ring-2` and `disabled:opacity-50`.
