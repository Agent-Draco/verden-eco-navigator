## 2024-06-08 - Accessible Icon-Only Toggle Buttons
**Learning:** Icon-only toggle buttons (like password visibility) need explicit `aria-label`, `aria-pressed`, and clear `focus-visible` styling to be usable by screen readers and keyboard users.
**Action:** Always add these attributes to icon buttons that toggle state.
