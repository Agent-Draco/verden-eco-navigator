## 2026-06-26 - Standardize Navigation Accessibility
**Learning:** Navigation components must always include semantic `aria-current="page"` on active routes and standardized `focus-visible` tailwind rings to ensure screen-reader and keyboard accessibility.
**Action:** Apply these standard ARIA and focus states to all custom interactive elements, like SidebarNav, BottomNav, and GlassButton.
