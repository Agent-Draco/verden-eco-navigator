## 2026-06-28 - Consistent Navigation Accessibility

**Learning:** Navigation elements lacking `aria-current` and keyboard focus outlines are common a11y regressions in custom UI components.
**Action:** Standardize `aria-current="page"` on active nav routes and ensure `focus-visible` utility patterns are applied to all interactive custom elements.
