## 2026-04-12 - Adding ARIA labels to Icon Buttons

**Learning:** Custom icon-only components, such as 'GlassButton' with 'Chevron' icons, frequently lack accessible labels by default which harms screen reader usability.
**Action:** Ensure that all icon-only action buttons are explicitly supplied with descriptive 'aria-label' attributes across all components and pages.
