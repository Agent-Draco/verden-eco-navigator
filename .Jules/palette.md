## 2025-06-30 - Standardizing Navigation Accessibility
**Learning:** Navigation components need semantic `aria-current="page"` attributes and distinct keyboard focus indicators (`focus-visible`) to be fully accessible to screen readers and keyboard users.
**Action:** Apply `aria-current="page"` for active routes and standardize focus rings across all navigation elements.
