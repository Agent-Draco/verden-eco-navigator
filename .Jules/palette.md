## 2026-06-27 - Consistent Keyboard Focus Indicators
**Learning:** Interactive elements in custom navigation components require explicit focus-visible styles (using ring) alongside semantic ARIA states like `aria-current='page'` for accessibility parity.
**Action:** Apply this focus-visible pattern and aria-current logic to all future navigation buttons across the project.
