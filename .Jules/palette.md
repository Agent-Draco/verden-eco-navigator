## 2025-02-28 - Accessible Navigation
**Learning:** Navigation menus frequently lack `aria-current` indicators and explicit keyboard focus states (`focus-visible`), hindering keyboard usability.
**Action:** Always add `aria-current="page"` and `focus-visible:ring` utilities to interactive navigation elements to ensure robust keyboard accessibility.
